# Environment Variables for AWS Lambda
# Copy these to your Lambda function's environment variables in AWS Console

# Application Environment
NODE_ENV=production

# Database Configuration
DB_HOST=your-db-host.amazonaws.com
DB_PORT=5432
DB_NAME=outlaw_admin
DB_USER=your-db-username
DB_PASSWORD=your-db-password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Session Configuration
SESSION_SECRET=your-session-secret-key

# AWS Configuration (usually auto-populated by Lambda)
AWS_REGION=us-east-1

# API Configuration
API_VERSION=v1
CORS_ORIGIN=*

# Security Configuration
BCRYPT_ROUNDS=12

# Third-party API Keys (if applicable)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# S3 Configuration (if using S3 for file uploads)
S3_BUCKET_NAME=your-s3-bucket-name
S3_REGION=us-east-1

# Email Configuration (if using SES or other email service)
EMAIL_FROM=noreply@yourdomain.com

# Logging Level
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Other service URLs
FRONTEND_URL=https://your-frontend-domain.com